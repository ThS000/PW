const task = {
    id: 301,
    title:  "Treino de LLM",
    status: "pendente"
}

console.log(task.title);

console.log(task.status);

task.priority = "alta";

console.log(task.priority);

task.status = "em_execucao";

console.log(task);
